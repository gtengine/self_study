// boolean

const amIFat = true;
console.log(amIFat);

const amIThin = false;
console.log(amIFat);

// null : 아무것도 정의되지 않음.
const myWeights = null;
console.log(myWeights);

// undefind : 변수는 존재하지만 값이 없음.
let something;
console.log(something, myWeights);